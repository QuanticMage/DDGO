<Window x:Class="DungeonDefendersOfflinePreprocessor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DungeonDefendersOfflinePreprocessor"
        mc:Ignorable="d"
        Title="Dungeon Defenders Offline Preprocessor" Height="800" Width="1200">
	<Grid Margin="12">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="8"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<DockPanel Grid.Row="0" LastChildFill="False">
			<Button Content="Process"
                    Padding="10,6"
                    Margin="0,0,10,0"
                    Click="Process_Click" />
		</DockPanel>

		<!-- "Console" -->
		<Border Grid.Row="2" BorderBrush="#CCC" BorderThickness="1" CornerRadius="6" Padding="6">
			<ListBox x:Name="LogList"
                     ItemsSource="{Binding LogLines}"
                     FontFamily="Consolas"
                     FontSize="12"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     ScrollViewer.CanContentScroll="True">
				<ListBox.ItemTemplate>
					<DataTemplate>
						<!-- Use TextBlock for speed; no wrapping like a console -->
						<TextBlock Text="{Binding}" />
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>
		</Border>
	</Grid>
</Window>
